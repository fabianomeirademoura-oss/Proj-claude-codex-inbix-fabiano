# Atalhos do Windows precisam de CRLF; planilhas são binárias (o Git não mescla .xlsx).
*.cmd  text eol=crlf
*.xlsx binary

# Gancho e scripts de shell rodam no bash (também no Git para Windows): sempre LF.
.githooks/*  text eol=lf
*.sh         text eol=lf
